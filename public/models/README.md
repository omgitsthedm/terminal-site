# Model Pipeline

- Put source model at `public/models/input.glb`
- Run `npm run model:draco`
- Optimized output: `public/models/output.draco.glb`

Tip: Keep source assets outside git for large files and track only final compressed GLB files.
